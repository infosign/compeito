"""Compeito — 1EdTech CASE v1.1 compatible web service.

`__version__` is the single source of truth for the released version. It is
mirrored in `pyproject.toml`; `tests/unit/test_version.py` fails if the two
drift apart.
"""

__version__ = "2.7.0"
